# DESIGN.md — 岁时历（Suishi Calendar）

> 本文档定义「岁时历」的视觉语言，供 AI 编码代理与设计师统一遵循。
> 设计基调：**东方纸墨 + 朱砂点睛**。暖纸白为底、墨色为骨、朱砂红为唯一的动作色，青玉与鎏金分别点缀节气与节日。
> 风格参考：Google Stitch DESIGN.md 规范（https://stitch.withgoogle.com/docs/design-md/specification/）。

---

## 1. Visual Theme & Atmosphere

- **氛围**：克制、留白、有东方书卷气，但不拟古、不堆砌装饰。信息密度中等，日历是绝对主角。
- **表面类型（Surface-First）**：月历首页是 **Monitor/Operate** 复合表面——用户既在"扫读状态"（今天、本月、天气），也在"执行动作"（切月、点日期）。因此**没有 Hero、没有三宫格卖点卡片**，用网格密度和层级说话。
- **设计哲学**：数字（公历日期）大而清晰是第一层级；农历/节气/节日是第二层级注解；天气是第三层级的补充信息。层级靠**字号、字重、颜色**区分，不靠加框加图标。

---

## 2. Color Palette & Roles

| Token | Hex | 角色 |
|-------|-----|------|
| `ink` | `#1F1B16` | 主文字（日期数字、标题），暖近黑 |
| `ink-soft` | `#5C554B` | 次级文字（农历、辅助说明） |
| `muted` | `#948B7E` | 弱化文字（占位、非本月日期） |
| `paper` | `#FAF7F2` | 页面背景（暖纸白） |
| `surface` | `#FFFFFF` | 卡片 / 格子表面 |
| `border` | `#E7E0D4` | 分割线 / 描边 |
| `vermilion` | `#C03A2B` | **唯一动作色**：今天、主按钮、选中态、重点天气 |
| `vermilion-soft` | `#F7E4E0` | 朱砂的浅底（今日格子背景、选中态浅底） |
| `jade` | `#4A7C59` | 节气标注（24 节气角标） |
| `gold` | `#A9801F` | 传统节日标注（春节/中秋等） |
| `weather-cold` | `#3B7CB8` | 天气·低温/雨雪类（功能性） |
| `weather-hot` | `#C0392B` | 天气·高温类（功能性） |

**对比度（WCAG）**：`ink` 对 `paper`/`surface` ≈ 15:1（AAA）；`vermilion`(#C03A2B) 对 `surface` ≈ 5.4:1（AA 正文达标，大字号 AAA）；`muted`(#948B7E) 对 `paper` ≈ 3.3:1，**仅限非关键辅助信息**，不用于正文。

**用色纪律**：
- 朱砂红**只**用于：今天、选中态、主按钮、关键天气警示。同一屏朱砂元素 ≤ 3 处。
- 青玉仅用于节气，鎏金仅用于节日，二者不混用、不挪作他用。
- 冷暖天气色只出现在天气组件内，不污染日历网格。

---

## 3. Typography Rules

| Token | Font | Size / Weight / Line-height | 用途 |
|-------|------|-----------------------------|------|
| `display` | "Iowan Old Style", "Songti SC", "Noto Serif SC", serif | 40px / 600 / 1.1 | 首页今日大数字、日详情公历大字 |
| `title` | system-ui, "PingFang SC", "HarmonyOS Sans SC", "MiSans", sans-serif | 20px / 600 / 1.3 | 年月标题 |
| `body` | 同上 sans | 16px / 400 / 1.5 | 正文、设置项 |
| `body-strong` | 同上 | 16px / 600 / 1.5 | 强调正文 |
| `label` | 同上 | 13px / 500 / 1.4 | 农历、节气、星期、天气温度 |
| `caption` | 同上 | 11px / 400 / 1.3 | 角标、弱化说明 |

**规则**：
- 中文全站用系统无衬线体（跨端一致、零字体包体积）；大日期数字用衬线体制造"纸墨"气质。
- 数字与日期一律用 `font-variant-numeric: tabular-nums`，保证网格对齐不跳动。
- 层级靠**字号 + 字重**，不靠颜色滥用、不靠加图标。

---

## 4. Component Stylings

| 组件 | 样式 |
|------|------|
| `card` | `surface` 底，`rounded: lg`(16px)，`border: 1px solid #E7E0D4`，无阴影（见 Depth） |
| `day-cell` | 默认 `surface` 底；`today` 态：`vermilion` 圆形底 + 白字；`selected` 态：`vermilion-soft` 底 + `ink` 字 + 朱砂描边 |
| `button-primary` | `vermilion` 底、白字、`rounded: full`、padding 12/20、按压态降 8% 亮度 |
| `segmented` | 浅底 `paper-deep`(#F1EBE2) 圆角容器，选中块白底 + 墨字 |
| `weather-card` | `card` + 左侧大温度数字（display 40px）+ 右侧天气图标与体感湿度风力 |
| `list-item`（设置） | 白底，16px body，右侧 chevron，分隔线 `border` |
| `chip`（节日/节气角标） | 圆角 full，文字 `label`(11px)，节=鎏金、气=青玉，仅文字不用底 |

**交互态**（均需定义）：
- `hover`：仅桌面端出现，`paper-deep` 浅底。
- `pressed`：主按钮亮度 -8%；格子轻微缩放 0.96。
- `disabled`：`muted` 文字 + `border` 底。

---

## 5. Layout Principles

- **间距尺度**：4 / 8 / 12 / 16 / 20 / 24 / 32（4px 基础）。
- **月历网格**：7 列等宽，格子高度 = 屏宽 / 7 的约 0.92 倍（近方形），周日列弱化（非本月）。
- **留白**：页面左右安全边距 16px；日历网格铺满但四周留 12px 呼吸。
- **一屏原则**：首屏 = 头部(年月+今天按钮) + 星期行 + 6×7 网格 + 今日天气卡，不滚动即完成"看日期+看天气"。
- **对齐**：数字左对齐、农历右对齐的格子内部结构保持一致，形成可扫读的列节奏。

---

## 6. Depth & Elevation

- **去阴影**：以"纸"为隐喻，卡片用 **1px 描边** 而非投影来分层；全站无重投影。
- **唯一可浮起元素**：切月时的月份过渡、日详情页的头部可给一层极轻投影 `0 1px 3px rgba(31,27,22,0.08)`。
- **层级靠底色深浅**：`paper`(底) → `surface`(卡片) → `paper-deep`(次级区)，而非靠阴影堆叠。

---

## 7. Do's and Don'ts

**Do**
- ✅ 日期数字大而清晰，农历作为 11px 注解在下方。
- ✅ 朱砂红严格收敛：今天 / 选中 / 主按钮，一屏 ≤ 3 处。
- ✅ 天气信息只在天气卡与日详情出现，不污染月历网格。
- ✅ 触摸目标 ≥ 44px（格子天然满足）。

**Don't**
- ❌ 不要渐变背景、玻璃拟态（无真实层级系统支撑）。
- ❌ 不要给每个格子加图标 / 加彩色边框。
- ❌ 不要彩虹色盘；冷暖天气色不出天气组件。
- ❌ 不要用投影堆出卡片；用描边。
- ❌ 不要在月历格子里塞进温度数字（拥挤且喧宾夺主），今日天气放在独立天气卡。

---

## 8. Responsive Behavior

- **手机竖屏为主**：375×812 为基准，缩放至 320–430 宽度自适配（网格用百分比/`rpx`）。
- **平板/横屏**：月历最大宽度 560px 居中，两侧留白；天气卡与日历并排（可用 grid 双列）。
- **深色模式**：`paper`→`#141210`、`surface`→`#1E1B17`、`ink`→`#F2EDE5`、`ink-soft`→`#B8AFA2`、`vermilion` 提亮至 `#E05745`，其余角色等比微调，保证对比度不变。

---

## 9. Agent Prompt Guide

给 AI 编码代理的速查与即用提示：

- 配色速查：主文字 `#1F1B16` · 背景 `#FAF7F2` · 卡片 `#FFFFFF` · 描边 `#E7E0D4` · 强调朱砂 `#C03A2B` · 节气青玉 `#4A7C59` · 节日鎏金 `#A9801F`。
- 即用提示：「按 `DESIGN.md` 实现月历首页：7 列网格、今天用朱砂圆形底、农历 11px 注于日期下方、节气用青玉文字角标、节日用鎏金文字角标、顶部今日天气卡、无投影卡片用 1px 描边。」
- 即用提示：「实现日详情页：公历大数字用衬线 display 字体、农历与干支/生肖/节气/节日分行、当日天气详情卡 + 7 日预报横向列表，仅朱砂红用于强调，遵循 4px 间距尺度。」
- 暗色模式替换：`#FAF7F2`→`#141210`、`#FFFFFF`→`#1E1B17`、`#1F1B16`→`#F2EDE5`、`#C03A2B`→`#E05745`。
