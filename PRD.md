# 产品需求文档（PRD）—— 岁时历（Suishi Calendar）

| 项目 | 内容 |
|------|------|
| 产品名 | 岁时历（Suishi Calendar） |
| 一句话定位 | 一个看日期、农历、节气、天气，一屏搞定的日历 |
| 平台 | Android / iOS（uni-app 跨端，HBuilderX 构建） |
| 版本 | v1.0 MVP |
| 状态 | Draft |
| 作者 | firecache |

---

## 1. 背景与目标

### 1.1 背景

- 系统日历与主流第三方日历中，农历信息被弱化为次要标注，节气、干支、传统节日要么缺失、要么需层层点开。
- 天气与日历是两个割裂的工具：用户"看日期"和"看天气"要来回切换两个 App。
- 华人用户有真实的农历生活场景：安排传统节日、看节气（穿衣/农事/养生）、择日、了解生肖干支。

### 1.2 目标（非目标）

**目标**
- 把「公历 + 农历 + 节气 + 节日 + 天气」收敛到一个首屏，减少工具切换。
- 农历为本地纯计算、离线可用；天气可联网、可降级。

**非目标（本版不做）**
- 不做事件/日程管理（可后续接入）。
- 不做黄历宜忌（择日）完整库（v1 仅占位，后续可扩展）。
- 不做账号体系与云同步。

---

## 2. 目标用户

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| 农历用户 | 习惯按农历/节气/传统节日生活的华人（以 30–55 岁为主） | 一眼看到"今天是几月初几、什么节气、什么节日" |
| 出行决策者 | 上班族、出差族，需要"日期 + 天气"对应 | 出门前看今天与未来几天天气，决定穿衣/出行 |

---

## 3. 用户问题与需求

| 问题 | 现状 | 本产品的解法 |
|------|------|--------------|
| 想知道某天农历与节气 | 系统日历信息不全 | 月历格子内直接标注农历、节气、节日 |
| 日期与天气割裂 | 要开两个 App | 首屏天气卡 + 每格今日天气图标，日详情带 7 日预报 |
| 传统节日易被忽略 | 无提示 | 节日/节气在月历中视觉突出标注 |

---

## 4. 产品范围与功能需求

### 4.1 功能优先级（MoSCoW）

**Must（v1 必须有）**
- [x] 月视图日历：6×7 网格，公历为主、农历为辅，今天高亮
- [x] 农历本地计算：农历年月日、干支纪年、生肖、24 节气、传统/法定节日
- [x] 今日天气卡片：天气现象、温度、体感、湿度、风力、空气质量
- [x] 日详情页：点某天 → 完整农历信息 + 该日天气 + 7 日预报
- [x] 月份切换：左右滑动 / 箭头，快捷回到"今天"
- [x] 设置：城市切换、周起始日（周一/周日）、主题（跟随系统/浅色/深色）

**Should（重要）**
- [ ] 公历/农历切换显示（农历为主视图）
- [ ] 天气数据缓存（离线展示最近一次结果）

**Could（锦上添花）**
- [ ] 深色模式更精细的配色
- [ ] 节日倒计时

**Won't（本版不做）**
- [ ] 日程/事件、提醒推送、桌面小部件、账号云同步、黄历宜忌完整库

### 4.2 页面结构

```
App
├── 月历首页 pages/index
│   ├── 头部：年份 · 月份 · 农历年/生肖 · 今天按钮
│   ├── 星期行（周一/周日起，可配）
│   ├── 6×7 日历网格（公历 + 农历 + 节气/节日角标，今日高亮）
│   └── 今日天气卡片（可点击进日详情）
├── 日详情页 pages/day
│   ├── 公历 + 农历大字
│   ├── 干支 / 生肖 / 节气 / 节日
│   ├── 当日天气详情
│   └── 7 日天气预报
└── 设置页 pages/settings
    ├── 城市选择
    ├── 天气数据源（API Key / 演示模式）
    ├── 周起始日
    └── 主题（跟随系统/浅色/深色）
```

### 4.3 关键业务规则

1. **农历计算**：纯前端本地算法（1900–2100 区间），无网络依赖。
2. **天气数据源**：默认「演示模式」（内置模拟数据，开箱即用）；接入和风天气（QWeather）后展示真实数据，Key 与城市可配置。
3. **今天判定**：以设备本地时区为准。
4. **周起始日**：默认周一，可在设置切换为周日。

---

## 5. 非功能需求

| 维度 | 要求 |
|------|------|
| 离线可用 | 农历/节气/节日本地计算，断网可用；天气降级为缓存或演示数据 |
| 性能 | 月历首屏渲染 < 1s；滑动切月流畅（无掉帧感） |
| 隐私 | 位置仅可选授权，用于自动定位城市；可手动选城市，不强制 |
| 兼容 | Android 8.0+ / iOS 13+；uni-app（Vue 3）跨端一致 |
| 体积 | 安装包尽量精简（无重型第三方依赖） |

---

## 6. 用户故事（INVEST）

- 作为用户，我打开 App 就能看到本月日历、今天天气和农历，以便快速规划。
- 作为用户，我点某个日期能看到该日农历、节气、节日和天气，以便安排传统活动或出行。
- 作为用户，我能看未来 7 天天气，以便决定穿衣和通勤方式。
- 作为用户，我能切换城市，以便出差/旅行时查看当地天气。
- 作为用户，我能把周起始日改成周日，以符合我的使用习惯。
- 作为用户，我能切换深色主题，以便夜间使用不刺眼。

---

## 7. 成功指标（North Star 与输入指标）

- **North Star**：周均每用户打开次数（WAPU，Weekly Active Opens）—— 用户是否把它当作高频日历入口。
- 输入指标：
  - 首屏天气卡点击率（→ 日详情转化）
  - 7 日预报查看率
  - 城市切换使用率（多地点场景是否被满足）
  - 次留 / 7 日留存

---

## 8. 里程碑与发布计划

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M1 · MVP（本版） | 月历 + 农历 + 今日天气 + 日详情 + 设置 | 上述 Must 全部可用，双端构建通过 |
| M2 · 增强 | 农历主视图、天气缓存、深色细化、节日倒计时 | Should/Could 项落地 |
| M3 · 生态 | 提醒推送、桌面小部件、黄历宜忌 | 单独立项评估 |

---

## 附：关键假设与待确认

1. 天气数据源默认用**和风天气（QWeather）**免费版，需用户自行申请 Key（MVP 用演示模式兜底）。
2. 产品名「岁时历」为暂定名，可随时替换（改动 `manifest.json` 的应用名 + 图标即可）。
3. v1 不含账号与云同步。
